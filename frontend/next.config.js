/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove the experimental object if it only contained appDir
  // If you have other valid experimental options, keep them
  // experimental: {
  //   // Remove the appDir option
  // },
};

module.exports = nextConfig;
